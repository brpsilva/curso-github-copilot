# Imagens pendentes para o tutorial

Use este checklist para completar os prints reais que ainda não existem em `img/`.

## Capturas recomendadas

- `img/vscode-dados-csv-aberto.png`: VS Code com `dados.csv` aberto, Explorer lateral visível e conteúdo do CSV no editor.
- `img/vscode-estrutura-projetos.png`: Explorer do VS Code mostrando `Projetos-Copilot` com as pastas `Projeto-CSV`, `Projeto-Texto`, `Projeto-Marketing` e `Projeto-Testes`.
- `img/copilot-chat-modos.png`: Copilot Chat com o seletor de modo aberto ou com Ask, Agent e Plan visíveis.

## Revisão de privacidade

- Revisar prints de convite, organização, perfil, conta e licença antes de publicar fora da aula.
- Ocultar e-mail, usuário, organização, caminhos locais e qualquer dado corporativo que não possa aparecer no material final.

## Como aplicar depois

1. Coloque as novas imagens em `img/` com os nomes acima.
2. Insira cada imagem no slide correspondente usando o padrão `.ss-block` já usado nas Partes 1 e 4.
3. Rode uma busca por `Print removido`, `Imagem demonstrativa removida` e `Próximo print recomendado` antes de finalizar.
